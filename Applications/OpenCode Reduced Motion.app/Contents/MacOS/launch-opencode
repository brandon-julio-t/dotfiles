#!/bin/sh

if /usr/bin/pgrep -x OpenCode >/dev/null 2>&1; then
  /usr/bin/osascript -e 'display notification "Quit OpenCode first, then open this launcher again." with title "OpenCode Reduced Motion"'
  exit 0
fi

exec /usr/bin/open -a OpenCode --args --force-prefers-reduced-motion
