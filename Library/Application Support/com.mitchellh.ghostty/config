theme = Cursor Dark
font-family = SF Mono
font-size = 12
adjust-cell-height=1

# workaround for ghostty nushell ssh problem for now https://github.com/ghostty-org/ghostty/issues/7877
env = TERM=xterm-256color

command = /opt/homebrew/bin/nu
shell-integration-features = ssh-terminfo,ssh-env,title,sudo,cursor
cursor-style = bar
cursor-style-blink = false
cursor-color = #2b7fff
adjust-cursor-thickness = 3
custom-shader = ~/repos/ghostty-cursor-shaders/cursor_warp.glsl

maximize = true
unfocused-split-opacity = 0.9
background-opacity = 0.85
background-blur = true

macos-window-buttons = hidden
macos-titlebar-proxy-icon = hidden

auto-update = download
auto-update-channel = tip
